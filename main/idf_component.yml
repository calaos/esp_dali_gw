dependencies:
  idf:
    version: ">=6.0,<7.0"

  # RMT-based DALI master (esp-iot-solution). Blocking and not thread-safe: only dali_bus calls it.
  espressif/dali: "^1.1.0"

  # esp-mqtt and cJSON left IDF in v6.0 and are managed components now.
  espressif/mqtt: "^1.1.0"
  espressif/cjson: "^1.7.19"

  espressif/mdns: "^1.9.1"
  espressif/led_strip: "^3.0.3"
