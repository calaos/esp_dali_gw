dependencies:
  idf:
    version: ">=6.0,<7.0"

  # No espressif/dali here: it is vendored at components/dali for listen mode (docs/adr/0005).

  # esp-mqtt and cJSON left IDF in v6.0 and are managed components now.
  espressif/mqtt: "^1.1.0"
  espressif/cjson: "^1.7.19"

  espressif/mdns: "^1.13.0"
  espressif/led_strip: "^3.0.3"
